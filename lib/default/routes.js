'use strict';

var tracks = require('trackfinder'),
	debug = require('debug')('anden:core'),
	path = require('path');

module.exports = function(anden){
	debug('[%s] routes running %s', anden.name);

};